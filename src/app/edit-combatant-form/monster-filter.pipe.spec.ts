import { MonsterFilterPipe } from './monster-filter.pipe';

describe('MonsterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MonsterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
